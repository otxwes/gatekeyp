# Core dependencies for Gatekeyp
# Phase 0: Standard library only (hashlib, secrets, sqlite3, unittest)
# Phase 1+: Security hardening dependencies

# Encryption at rest (Phase 1)
cryptography>=42.0.0

# Key derivation / password hashing (Phase 1)
argon2-cffi>=23.1.0

# Web framework for the API gateway (Phase 1+)
# fastapi>=0.110.0
# uvicorn[standard]>=0.29.0

# Testing
pytest>=8.0.0